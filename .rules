# MOMO 项目 - 规则

## 项目本质
MOMO 是面向中文用户的场景化 AI 情绪陪伴 App。
核心场景：深夜低落、情绪内耗、关系复盘、压力峰值、孤独陪伴。
不是治疗产品，不是角色扮演产品，不是泛聊天产品。

## 仓库结构（monorepo）
- apps/mobile: React Native (Expo) 移动 App
- apps/web: Next.js 抢先体验页（一页介绍 + 邮箱收集）
- services/api: FastAPI 后端（Python 3.12 + uv）
- packages/shared-types: 前后端共享类型

## 技术栈
- 后端：Python 3.12 + FastAPI + Pydantic + Postgres + Redis
- 前端：React Native (Expo) + TypeScript + Zustand + React Query
- LLM：DeepSeek（主）+ 豆包（备份），通过 provider 抽象层切换
- 部署：阿里云 ECS + Docker（一开始单机即可）

## 代码约定
- 后端按业务领域切分目录（domain/conversation、domain/memory 等），不要按技术类型
- 所有 LLM 调用必须经过 app/llm/ 的 provider 接口，不要直接调 SDK
- 所有用户输入和模型输出必须经过 app/domain/safety/ 的内容审核
- Pydantic 模型用于所有 API schema 和 LLM 结构化输出
- 数据库迁移用 Alembic（明天加）
- 前端通过 React Query 管理服务端状态，本地状态用 Zustand
- 移动端用 Expo OTA，避免每次小改都过 App Store 审核

## 不要做的事
- 不要用同步 HTTP 客户端（必须用 httpx async）
- 不要在业务代码里硬编码模型名（写在 config 里）
- 不要把秘钥写进代码（用 .env）
- 不要做未成年人主场景，所有 UI 都默认成年人
- 不要用任何"治疗""诊断"措辞，统一用"情绪支持/陪伴/自我整理"
- 不要在没有 type hint 的情况下写 Python 函数
- 不要在没有 TypeScript 类型的情况下写前端函数
- 不要直接 import 未抽象的 LLM SDK（用 app/llm/ 的接口）

## 验证命令
- 后端格式化：cd services/api && uv run ruff format .
- 后端类型检查：cd services/api && uv run mypy app
- 后端测试：cd services/api && uv run pytest
- 后端启动：cd services/api && uv run uvicorn app.main:app --reload
- 移动端启动：cd apps/mobile && pnpm start
- 网页启动：cd apps/web && pnpm dev

## 关键业务概念（写代码必须理解）
- 场景 (Scene)：深夜低落、情绪内耗、关系复盘、压力峰值、孤独陪伴 - 用户进入 App 时选择
- 人格卡 (Persona)：MOMO 的稳定语气、边界、价值观、用户称呼
- 情景记忆 (Episodic Memory)：事件、情绪、触发点、回应偏好（不是简单对话历史）
- 回应强度 (Response Intensity)：轻回应 / 陪伴 / 复盘 / 建议 / 行动 - 不是越多越好
- 聊后整理 (Reflection Card)：情绪命名、触发点、下一步轻动作
- 安全降级 (Safety Fallback)：危机表述时从陪伴模式切到现实支持
